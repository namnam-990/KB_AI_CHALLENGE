"""TODO: 실제 테이블 모델 정의 (User, Document, CreditFactor, Account, Transaction 등).

예시 스케치:

class User(Base):
    __tablename__ = "users"
    id: Mapped[str] = mapped_column(primary_key=True)
    phone: Mapped[str]
    visa_type: Mapped[str | None]
    visa_expiry_date: Mapped[date | None]
"""
